/*int n;
    cin >> n;
    vector<int>v;
    /* 
    v.push_back(100);
    v.push_back(500);
    v.push_back(200);
    v.push_back(120); 
    */
   /* int a;
    for(int i = 0; i < n; i++){
        cin >> a;
        v.push_back(a);
    }
    //ascending order me sort hoga
    sort(v.begin(), v.end());
    //descending order me sort hoga
    // sort(v.begin(), v.end(), greater<int>());
    for(auto x : v){
        cout << x << " ";
    }
    cout << endl;

    */
   /*
    vector<int> primeRange(int m, int n) {
        vector<int>v1;
        for(int i = m; i<=n; i++){
            if(i==1){
                continue;
            }
            if(isPrime(i)){
                v1.push_back(i);
            }
        }
        return v1;
    }*/